This script will reset the font cache for all users and the system font cache. It's also available as a payload-free package.

Once the font caches have been removed, the Apple Type Services service is stopped and restarted.
